export type User = {
    id: number
    name: string
    email: string
    phone: string
    password?: string
    description?: string
    linkedin: string
    github: string
    email_verified_at: boolean
    isAdmin: number
    created_at: string
    updated_at: string
}